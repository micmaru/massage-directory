# SPA HAS 8 SECTIONS — S9 OWNERSHIP DISSOLVED

Status: locked

Source: Session Log — 16 June 2026, decisions 1-10

Supersedes the 9-section spa structure recorded in the compiled decision record.

1. S9 Ownership dissolved — ownership fields distributed into S1 and S2. Spa has 8 sections total, same as therapist.
2. Spa S1 — Business Info (read-only, admin-only changes): `displayName`, `tradingName`, `registrationNumber`, `vatNumber`, `ownerFirstName`, `ownerLastName`, `ownerMobile`.
3. Spa S2 — Management & Contact (editable by spa): `managerFirstName`, `managerLastName`, `managerMobile`, `cellNumber` (read-only), `spaPhone`, `spaPhoneVisible`, `spaMobile`, `spaMobileVisible`, `email`, `whatsappNumber`, `showWhatsapp`, `contactPreferences`, `requiresInvoice`.
4. Manager fields are editable by the spa — managers change. Owner fields are read-only / admin-only, locked post-registration.
5. S4 for spa = amenities only. Spas have no `premisesType`; the note on the diagram is a reminder only, not a spa field.
6. M4-1(b) exit flow is identical to M3-1(b) — same for both supplier types.
