# OTP — ONE SHARED MECHANISM

Status: locked

Source: Session Log — 7 July 2026, locked decision 2

OTP verification is ONE shared mechanism used by all four supplier entry points: M1 (therapist registration), M3b (therapist dashboard), M2 (spa registration), M4 (spa dashboard).

Built once, reused identically — never duplicated per supplier type.

PARKED (8 July): extracting it into a single `otp.html` / `otp-component.js` instead of the current per-screen duplication — pre-launch cleanup candidate, not urgent.
